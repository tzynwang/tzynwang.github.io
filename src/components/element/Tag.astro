---
interface Props {
  tag: string;
}

const { tag } = Astro.props;
const tagUrl = `/tag/${tag}`;
---

<a
  class="relative inline-flex items-center bg-teal-800 px-2 py-1 text-sm text-neutral-50 transition-colors dark:bg-teal-300 dark:text-neutral-800"
  href={tagUrl}
>
  <slot />
</a>
